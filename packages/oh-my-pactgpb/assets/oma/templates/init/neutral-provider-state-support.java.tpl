package {{PACKAGE_NAME}};

class {{STATE_SUPPORT_CLASS_NAME}} {

  // Optional bootstrap shell only.
  // Add concrete @State("...") handlers here once real pact interactions are available.
  // Keep this class neutral until artifact source and interaction semantics are grounded.
}
