# Explicit unsupported over implicit guessing

When the pack cannot support a requested action, report the unsupported boundary clearly and recommend the next safe command instead of proceeding implicitly.
