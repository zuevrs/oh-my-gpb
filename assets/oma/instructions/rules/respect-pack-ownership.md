# Respect pack ownership boundaries

The pack owns only `.oma/**`, namespaced `akita-*` paths under `.opencode/**`, and its managed blocks in `AGENTS.md` and `opencode.json`. Do not silently overwrite anything else.
